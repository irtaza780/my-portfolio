/**
 * Shared motion primitives.
 * The easing is typed as a fixed 4-tuple so Framer Motion's strict
 * `Easing` types accept it (a plain number[] is rejected).
 */
export const EASE_OUT: [number, number, number, number] = [0.22, 1, 0.36, 1];
